{% extends 'blocks/block_container.html.twig' %}
{% block content %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.createrID) }}
    {{ form_row(form.file) }}
    <button type="submit" class="btn btn-primary">{{ buttonName|default('Сохранить') }}</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('card.stocks.numbers', {'stockID': stock.id}) }}">Отменить</a>
    {{ form_end(form) }}
{% endblock %}

