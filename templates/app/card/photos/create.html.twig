{% extends 'base.html.twig' %}

{% block title %}Добавление фотографии{% endblock %}
{% block header %}{{ block('title') }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a
                href="{{ path('card.parts', {'page' : app.session.get('page/zapCards')|default(1)}) }}">Номенклатура</a>
    </li>
    <li class="breadcrumb-item"><a href="{{ path('card.parts.photos', {'id': zapCard.id}) }}">Фотографии {{ zapCard.number.value }}</a></li>

    <li class="breadcrumb-item">Добавление</li>
{% endblock %}

{% block body %}
    {% include ('app/card/parts/_tabs.html.twig') %}

    {{ include('app/card/photos/_form.html.twig', {'buttonName': 'Добавить'}) }}
{% endblock %}