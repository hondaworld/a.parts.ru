{% extends 'blocks/block_container.html.twig' %}
{% block content %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {% if not zapSkladLocation|default(null) %}
        {{ form_row(form.zapSkladID) }}
    {% endif %}
    {{ form_row(form.locationID) }}
    {{ form_row(form.quantityMin) }}
    {{ form_row(form.quantityMinIsReal) }}
    {{ form_row(form.quantityMax) }}

    <button type="submit" class="btn btn-primary">{{ buttonName|default('Сохранить') }}</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('card.parts.sklad', {'id' : zapCard.id}) }}">Отменить</a>
    {{ form_end(form) }}

{% endblock %}