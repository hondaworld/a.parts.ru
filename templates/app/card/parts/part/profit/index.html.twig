{% extends 'blocks/block_container_half.html.twig' %}
{% block header %}
    <div class="block-header block-header-default">
        <h3 class="block-title">Цена реализации по умолчанию</h3>
        <div class="block-options">
            <a type="button" class="btn btn-sm btn-alt-primary"
               href="{{ path('card.parts.profit', {'id': zapCard.id}) }}"
               title="Изменить цены реализации по умолчанию"><i class="fas fa-edit"></i></a>
            <a type="button" class="btn btn-sm btn-alt-primary"
               href="{{ path('card.parts.profitAll', {'id': zapCard.id}) }}"
               title="Изменить все наценки"><i class="fas fa-ruble-sign"></i></a>
        </div>
    </div>
{% endblock %}
{% block content %}
    <p><em>Будет использована при реализации товара, если цена не найдена в текущем прайс-листе и не выставлены наценки для данного товара</em></p>
    <p><strong>Цена реализации.: </strong> {{ zapCard.price1 }} руб.</p>
    <p><strong>Наценка: </strong> {{ zapCard.profit }}% <span class="text-muted font-italic">(используется, если цена не задана)</span></p>
{% endblock %}