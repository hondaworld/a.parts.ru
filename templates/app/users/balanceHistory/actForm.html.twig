{% extends 'blocks/block_container_full.html.twig' %}
{% block content %}
    {{ form_start(act, {'attr': {'novalidate': 'novalidate', 'target': '_blank'}, 'action': path('users.balance.history.act', {'userID': user.id})}) }}
    <div class="row">
        <div class="col">{{ form_widget(act.dateofadded) }}</div>
        <div class="col">{{ form_widget(act.firmID) }}</div>
        <div class="col"><button type="submit" class="btn btn-primary btn-sm">{{ buttonName|default('Акт') }}</button></div>
    </div>
    {{ form_end(act) }}
{% endblock %}
