{% extends 'blocks/block_container.html.twig' %}

{% block header %}
    {{ include ('blocks/block_header_default.html.twig', {'header': 'Рассылка прайс-листов'}) }}
{% endblock %}

{% block content %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.email_price) }}
    {{ form_row(form.zapSkladID) }}
    {{ form_row(form.isPrice) }}
    {{ form_row(form.isPriceSummary) }}

    <button type="submit" class="btn btn-primary">Сохранить</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('users.dop', {'id': user.id}) }}">Отменить</a>
    {{ form_end(form) }}

{% endblock %}