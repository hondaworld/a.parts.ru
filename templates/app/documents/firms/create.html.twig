{% extends 'base.html.twig' %}

{% block title %}Добавление документа организации {{ firm.nameShort }}{% endblock %}
{% block header %}{{ block('title') }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('firms') }}">Организации</a></li>
    <li class="breadcrumb-item"><a href="{{ path('firms.edit', {'id': firm.id}) }}">{{ firm.nameShort }}</a></li>
    <li class="breadcrumb-item"><a href="{{ path('firms.documents', {'firmID': firm.id}) }}">Документы</a></li>
    <li class="breadcrumb-item">Добавление</li>
{% endblock %}

{% block body %}
    {% include ('app/firms/_tabs.html.twig') %}
    {{ include ('app/documents/firms/_form.html.twig', {'buttonName': 'Добавить'}) }}
{% endblock %}