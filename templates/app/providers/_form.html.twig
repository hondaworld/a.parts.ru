{% extends 'blocks/block_container.html.twig' %}
{% block content %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.name) }}
    {{ form_row(form.zapSkladID) }}
    {{ form_row(form.user) }}
    {{ form_row(form.koef_dealer) }}
    {{ form_row(form.isDealer) }}

    <button type="submit" class="btn btn-primary">Добавить</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('providers', {'page' : app.session.get('page/providers')|default(1)}) }}">Отменить</a>
    {{ form_end(form) }}

{% endblock %}