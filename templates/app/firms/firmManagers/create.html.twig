{% extends 'base.html.twig' %}

{% block title %}Добавление сотрудника в организацию {{ firm.nameShort }}{% endblock %}
{% block header %}{{ block('title') }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('firms') }}">Организации</a></li>
    <li class="breadcrumb-item"><a href="{{ path('firms.edit', {'id': firm.id}) }}">{{ firm.nameShort }}</a></li>
    <li class="breadcrumb-item"><a href="{{ path('firms.managers', {'firmID': firm.id}) }}">Сотрудники</a></li>
    <li class="breadcrumb-item">Добавление</li>
{% endblock %}

{% block body %}
    {{ include('app/firms/firmManagers/_form.html.twig', {'buttonName': 'Добавить', 'showManager': true}) }}
{% endblock %}