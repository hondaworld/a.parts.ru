{% extends 'blocks/block_container.html.twig' %}

{% block header %}
    {{ include ('blocks/block_header_default.html.twig', {'header': 'Документ'}) }}
{% endblock %}

{% block content %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.document_prefix) }}
    {{ form_row(form.document_sufix) }}
    {{ form_row(form.dateofadded) }}
    {{ form_row(form.comment) }}

    <button type="submit" class="btn btn-primary">Сохранить</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('schets.show', {'id': schet.id}) }}">Отменить</a>
    {{ form_end(form) }}

{% endblock %}