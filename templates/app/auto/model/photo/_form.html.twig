{% extends 'blocks/block_container.html.twig' %}
{% block content %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.photo) }}
    <button type="submit" class="btn btn-primary">{{ buttonName|default('Сохранить') }}</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('auto.model.show', {'auto_markaID': autoMarka.id, 'id': autoModel.id}) }}">Отменить</a>
    {{ form_end(form) }}
{% endblock %}

