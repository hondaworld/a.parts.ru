{% extends 'blocks/block_container.html.twig' %}
{% block content %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.moto_groupID) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary">{{ buttonName|default('Сохранить') }}</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('auto.moto.model', {'auto_markaID': autoMarka.id}) }}">Отменить</a>
    {{ form_end(form) }}
{% endblock %}

