{% extends 'blocks/block_container.html.twig' %}
{% block content %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_row(form.moto_modelID) }}
    {{ form_row(form.vin) }}
    {{ form_row(form.number) }}
    {{ form_row(form.year) }}


    <button type="submit" class="btn btn-primary">{{ buttonName|default('Сохранить') }}</button>
    <a class="ml-3 btn btn-secondary" href="{{ path('users.moto', {'userID' : user.id}) }}">Отменить</a>
    {{ form_end(form) }}

{% endblock %}