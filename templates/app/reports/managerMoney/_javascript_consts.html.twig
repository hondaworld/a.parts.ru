<script>
    const labels = [
        {% for date in dates %}
        '{{ date|date(period == 'year' ? 'Y' : (period == 'month' ? 'm.Y' : 'd.m')) }}',
        {% endfor %}
    ];
    const profits = {
        {% for managerID, managerProfit in profits %}
        '{{ managerID }}': [
            {% for date in dates %}
            {{ managerProfit.date[date|date(period == 'year' ? 'Y' : (period == 'month' ? 'Y-m' : 'Y-m-d'))]|default(0) }},
            {% endfor %}
        ],
        {% endfor %}
    };
</script>