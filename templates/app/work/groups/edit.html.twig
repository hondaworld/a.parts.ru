{% extends 'base.html.twig' %}

{% block title %}Изменение группы работ {{ workGroup.name }}{% endblock %}
{% block header %}{{ block('title') }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('work.categories') }}">Категории работ</a></li>
    <li class="breadcrumb-item"><a href="{{ path('work.groups', {'workCategoryID': workCategory.id}) }}">{{ workCategory.name }}</a></li>
    <li class="breadcrumb-item">{{ workGroup.name }}</li>
{% endblock %}

{% block body %}
    {{ include('app/work/groups/_formEdit.html.twig') }}
{% endblock %}